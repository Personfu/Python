============================================================
CIS 195 — Web Development | Complete Coursework
Author: Preston Furulie
============================================================

------------------------------------------------------------
Assignment 1: HTML/CSS Portfolio Page
------------------------------------------------------------
File: portfolio.html

Semantic HTML structure with CSS flexbox layout and custom styling.
Built a responsive portfolio page featuring:
  - Dark theme with card-based project layout
  - Flexbox-based responsive grid (wraps on mobile)
  - Hover animations on project cards
  - Tag badges for technologies used

Working Process:
  1. Created semantic HTML with header, section, and div elements
  2. Applied CSS custom properties for consistent theming
  3. Used flexbox with flex-wrap for responsive card grid
  4. Added hover transform for interactive card elevation


------------------------------------------------------------
Assignment 2: JavaScript DOM Manipulation
------------------------------------------------------------
File: dom_manipulation.js

Dynamic task list with add/remove functionality:
  - Array-based task storage with unique IDs (Date.now())
  - renderTasks() clears and rebuilds DOM on every change
  - Click events toggle task completion
  - Form submit event adds new tasks

Working Process:
  1. Defined tasks array and task object structure {id, text, completed}
  2. renderTasks() iterates tasks, creates <li> elements dynamically
  3. toggleTask() finds task by id and flips completed boolean
  4. Form event listener prevents default submit, validates input


------------------------------------------------------------
Assignment 3: Responsive Design & Media Queries
------------------------------------------------------------
File: responsive_design.css

Mobile-first CSS Grid layout with 4 breakpoints:
  - Base (mobile): single column
  - 600px (tablet): 2 columns
  - 1024px (desktop): 3 columns with sidebar
  - 1400px (large): 4 columns with enhanced spacing

Working Process:
  1. Started with mobile-first single-column grid
  2. Added @media (min-width) breakpoints for progressive enhancement
  3. Used grid-template-columns for layout control at each breakpoint
  4. Applied grid-column spanning for full-width widgets


------------------------------------------------------------
Assignment 4: REST API Integration
------------------------------------------------------------
File: rest_api.js

Fetching and displaying data from JSONPlaceholder API:
  - async/await pattern for clean asynchronous code
  - Error handling with try/catch
  - Dynamic DOM card creation for each user
  - POST request example with JSON body

Working Process:
  1. Used fetch() with async/await to GET /users endpoint
  2. Checked response.ok before parsing JSON
  3. forEach loop creates card elements for each user
  4. Separate createPost() function demonstrates POST with headers

============================================================
END OF CIS 195 COURSEWORK
============================================================
